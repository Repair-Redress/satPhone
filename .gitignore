# === Runtime data (never pushed, never overwritten by pulls) ===
# These files are created at runtime and live only on the device.
# git ignores them, so `git pull` will never touch or delete them.

# Generated thermal images
thermal_output/*
!thermal_output/.gitkeep

# MMS outbox (Tasker picks up and deletes these)
.mms_outbox/

# SQLite database (users, credits, rate limits)
*.db

# Rotating log files
*.log

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Reference docs (download separately if needed)
*.pdf
